fastapi>=0.111.0    # Our web framework - handles HTTP requests/responses
uvicorn[standard]>=0.30.0   # Web server that runs our FastAPI app
pytest>=8.2.0   # Testing frame work to make sure our code works
httpx>=0.27.0   # For testnig HTTP requests to our API
